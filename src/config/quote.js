/**
 * Quote-request form configuration.
 *
 * ── PASTE YOUR WEB3FORMS ACCESS KEY BELOW ────────────────────────────
 * Create it free at https://web3forms.com — enter your email, they send
 * the access key instantly. Submissions then arrive in your inbox with
 * hCaptcha verified server-side by Web3Forms. Until the key is pasted,
 * the quote page shows direct contact info instead of the form.
 */
export const WEB3FORMS_ACCESS_KEY = '';

/** Web3Forms' shared hCaptcha site key (their docs) — no hCaptcha account needed. */
export const HCAPTCHA_SITEKEY = '50b2fe65-b00b-4b9e-ad62-3ba471098be2';

/** Business inquiries only — set up on the thejumpvault.com domain. */
export const CONTACT_EMAIL = 'alex@thejumpvault.com';
