# Fonts

The game uses system font fallbacks by default (works offline on Linux Mint).

To add self-hosted Nunito or Fredoka One, place `.woff2` files here and uncomment
the `@font-face` rules in `src/styles.css`.
